import { generatedYouLeadApiTypes } from "./generated-you-lead-api-types";

describe("generatedYouLeadApiTypes", () => {
  it("should work", () => {
    expect(generatedYouLeadApiTypes()).toEqual("generated-you-lead-api-types");
  });
});
